# Trial ended

Simple HTML + SCSS page.

    npm install
    npm run build    # compile scss -> css/styles.css
    npm run watch    # recompile on change

Then open `index.html`. Change the admin email in the button's `mailto:` link.
